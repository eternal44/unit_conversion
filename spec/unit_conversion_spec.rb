require 'spec_helper'

describe UnitConversion do
  it 'has a version number' do
    expect(UnitConversion::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
